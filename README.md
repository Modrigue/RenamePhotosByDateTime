# RenamePhotosByDateTime

Python script to rename photos by from the EXIF's Date + Time.

Usage:

Windows:

```Windows
python rename_photos_by_datetime.py <directory_path>
```

Linux:

```Linux
python3 rename_photos_by_datetime.py <directory_path>
```
